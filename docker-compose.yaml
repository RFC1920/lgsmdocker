services:
  ubuntu:
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    network_mode: host
    volumes:
      - /data:/data:ro
      - /home/rustserver:/home/ubuntu

